---

# cannot test with docker: 'ansible_default_ipv4' is undefined
driver:
  name: vagrant
vagrant:
  platforms:
    - name: xenial64
      box: bento/ubuntu-16.04
  providers:
    - name: virtualbox
      type: virtualbox
  instances:
    - name: vagrant-01

#driver:
#  name: docker
#docker:
#  containers:
##    - name: ansible-role-ufw-1404
##      image: ubuntu
##      image_version: 14.04
##      volume_mounts:
##        - ${MOLECULE_APTPROXY_PATH}:/etc/apt/apt.conf.d/00aptproxy
#
#    - name: ansible-role-ufw-1604
#      image: ubuntu
#      image_version: 16.04
#      volume_mounts:
#        - ${MOLECULE_APTPROXY_PATH}:/etc/apt/apt.conf.d/00aptproxy

ansible:
  playbook: tests/playbook.yml
  group_vars:
    all:
